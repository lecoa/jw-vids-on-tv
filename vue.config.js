module.exports = {
  transpileDependencies: ['vuetify'],
  // When deploying to a subdirectory (e.g. https://larrindell.com/jwvids/),
  // set publicPath to the subdirectory so asset URLs are generated correctly.
  publicPath: '/jwvids/',
};
